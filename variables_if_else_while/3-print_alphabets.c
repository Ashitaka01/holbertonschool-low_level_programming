#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * prints the alphabet in lowercase,
 * and then in uppercase, followed by a new line.
 * Return: 0
 */
int main(void)
{
        char a = 'a';

        while (a <= 'z')
        {
                putchar(c);
                c++;
        }

        char a = 'A';

        while (a <= 'Z')
        {
                putchar(c);
                c++;
        }

        putchar('\n');
        return (0);
}
