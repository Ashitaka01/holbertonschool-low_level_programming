#include <time.h>
#include <stdlib.h>
#include <stdio.h>
/**
 * main - main block
 * Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
int main(void)
{
        char a = '0';

        while (a <= '10')
        {
		              

                    putchar(a);

                
                    a++;
        }

        putchar('\n');
        return (0);
}
