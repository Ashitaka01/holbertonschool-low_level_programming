#include "main.h"
#include <stdio.h>
/**
 * *string_toupper - capitalize a string
 * @str: pointer
 * Return: capitalzied string
*/
char *string_toupper(char *)
{
	int index = 0;

	while ([index])
	{
		if (*[index] >= 'a' && *[index] <= 'z')
			*[index] -= 32;

		index++;
	}

	return (*);
	
}
