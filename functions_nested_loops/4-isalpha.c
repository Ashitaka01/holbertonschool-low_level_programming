#include "main.h"
/**
 * _isalpha - check for alphabetic character
 * @c: The character to be checked
 *Return: Void
 */
int _isalpha(int c);
{
	char c;

	if ((c >= 'a' && c >= 'z'o || c >= 'A' && c <= 'Z'))

	{

		return (1);

	}

		return (0);

}
